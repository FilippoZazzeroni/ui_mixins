name: example
description: A new Flutter project.
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.16.1 <3.0.0"


dependencies:
  flutter:
    sdk: flutter

  ui_mixins:
    path: "../../ui_mixins"
  bloc_eazy:
    git: "https://github.com/FilippoZazzeroni/bloc_eazy.git"

dev_dependencies:
  flutter_test:
    sdk: flutter


  flutter_lints: ^1.0.0

flutter:
  uses-material-design: true

